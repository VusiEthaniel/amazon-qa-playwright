# Amazon Clone – QA Automation (Playwright)

This project contains automated UI tests for an Amazon clone application using Playwright and JavaScript.

## Features Tested
- User login (valid & invalid)
- Add item to cart
- Remove item from cart

## Tools Used
- Playwright
- JavaScript
- Node.js

## How to Run
1. Clone the repo
2. Install dependencies:
   npm install
3. Ensure the Amazon clone app is running locally
4. Run tests:
   npx playwright test
